<% unless @association %>
  <%= render :partial => 'papers/search_form', :locals => { :klass => 'project' } %>
  <%= link_to t("new_#{controller_name.singularize}"), new_resource_path, :class => 'new button icon add' %>

  <ul class='button-group'>
    <li><%= link_to t('actual'), actual_admin_projects_path, :class => "button #{is_active('actual')}" %></li>
    <li><%= link_to t('asserted'), asserted_admin_projects_path, :class => "button #{is_active('asserted')}" %></li>
    <li><%= link_to t('deflected'), deflected_admin_projects_path, :class => "button #{is_active('deflected')}" %></li>
  </ul>
<% end %>

<%= render :partial => 'admin/papers/papers_list', :locals => { :collection => collection }%>
