<%= render :partial => 'admin/documents/view', :locals => { :resource => @subject } %>

<%= render :partial => 'admin/papers/papers_list', :locals => { :collection => collection }%>

<%= render :partial => 'admin/papers/search_form',
           :locals => {
             :klass => 'project',
             :subject => @subject, :association => controller_name
           } %>

<div class="search_result_block">
</div>
